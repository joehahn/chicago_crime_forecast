
The following will train some ML models to forecast crimes across Chicago.

Begin by loading file data/crimes_filtered.csv into object df_filtered

Then start with df_filtered and rename the 'date' column as 'timestamp'.
And derive the month of the year from the timestamp column and name that new column 'month'. 
Also derive from the timestamp column the rolling week-of-the-year such that every week includes 7 distinct dates, and name that column 'week'. 
Then preserve only those records whose primary_type are among the top 20.
Store results in object df_20. How many records are in df_20?

Display counts of primary_type in df_20

Display 1 random record in df_20, show all columns

Group df_20 by year, month, week, ward, primary_type and calculate 
    date(timestamp) as 'date',  
    count(distinct(date(timestamp))) as 'days_per_week'
    mean(arrest), 
    mean(domestic), 
    mean(latitude), 
    mean(longitude), 
    count(id) as 'count_0', 
Drop 'min_', 'max_, 'mean_' from all new columns. 
Cast ward column as integer. 
Reorder columns so that date is first and count_0 is last.
Order all records by date, ward, primary_type.
Name this new result df_avg. How many records are in df_avg?

Which ward in df_avg  has the greatest sum(count_0) among primary_type=THEFT records in df_avg?
Print every single record in df_avg that has primary_type=THEFT and ward=42 and year=2025

Which ward has the smallest sum(count_0) among primary_type=BURGLARY records in df_avg?


Show all records in df_avg having primary_type=BURGLARY and ward=22 and year=2025

12 use the year, month, week, ward, primary_type columns to determine which records are missing from df_avg. 
Zero-pad those missing records in df_avg  with count_0=0. 
Order all records by date, ward, primary_type.
Store that result in object df_pad.
How many records are in df_pad?

13 show all records in df_pad having primary_type=BURGLARY and ward=22 and year=2025

14 for each column in df_pad, find the NaN values and replace them with random selections of the non-NaN values,
with that result stored in df_nan

14 show all records in df_nan having primary_type=BURGLARY and ward=22 and year=2025

15 Print 5 random records in df_nan

16 partition df_nan by year, month, week, ward, primary_type and order by date.
Append a new column count_1 which is count_0 shifted forwards in time by 1 week.
Also append a new column count_2 which is count_0 shifted forwards in time by 2 weeks.
And append another new column count_3 which is count_0 shifted forwards in time by 3 weeks.
Dont drop any records due to missing data, instead use NaN to indicate missing future data.
Name that result df_target

17 show all records in df_target that have primary_type=THEFT and ward=27 and year>=2025

18 append a new column to df_target called 'ran_num' that has random floating point values 
uniformly distributed between 0 and 1, name that result df_ttv.
The create a new column 'TTV' in df_ttv
such that all rows having date <= '2025-04-01' and ran_num <= 0.667 have TTV='train',
          all rows having date <= '2025-04-01' and ran_num  > 0.667 have TTV='test',
      and all rows having date >  '2025-04-01' have TTV='validate'

20 show all records in df_ttv having primary_type=BURGLARY and ward=22 and year>=2024

21 let df_train = all records in df_ttv having TTV='train', keeping only the 
year, month, week, ward, primary_type, count_0, count_1, count_2, count_3 columns.
let df_test = all records in df_ttv having TTV='test', preserving the same columns. 
And let df_validate = all records in df_ttv having TTV='validate', preserving the same columns. 
How many records are in df_train, df_test, df_validate?

22 show 5 random records from df_train

23 train an XGBoost machine learning model on df_train. That model's features are the
year, month, week, ward, primary_type, count_0 columns, and that model's target variable is count_1,
with df_test being the testing sample of data. name that trained model model_1.
Then train an XGBoost on df_train using the same features to predict count_2, name that trained model model_2.
And also train an XGBoost on df_train using the same features to predict count_3, name that trained model model_3.

24 let df_predict = df_validate. Use model_1 to generate predictions on df_predict, and append those predictions to
df_predict as new column called 'count_1_pred'. Then do similar using model_2 and model_3

25 show all records in df_predict having primary_type=THEFT and ward=27

26 use df_predict to create a dashboard of plots that compare model predictions to actuals:

plot 1: scatterplot of model_1 predictions versus actuals. 
Use logarithmic axes ranging from 0.8 to 120.
Do not distinguish between the different crimes or wards.
Do not add 0.5 to the predictions or actuals.
Only include points whose predictions and actuals are > 0
Also overplot y=x via a dashed line

plot 2: same as plot 1 but for model_2

plot 3: same as plot 1 but for model_3

plot 4: show timeseries plots of summed actual and predicted THEFT, NARCOTICS, and ARSON versus date for model_1
using different colors for the different crimes. The plot's vertical axis should use logarithmic axis

plot 5: same as plot 4 but for model_2 

plot 6: same as plot 4 but for model_3

plot 7: show timeseries plots of summed actual and predicted for wards 27, 32, 38 versus date for model_1
using different colors for the different crimes. The plot's vertical axis should use logarithmic axis

plot 8: same as plot 7 but for model_2

plot 9: same as plot 7 but for model_3

plot 10: timeseries plot of count_0 versus date for THEFT in ward 27,
as well as date + 1 week versus count_1_pred
and date + 2 week versus count_2_pred
and date + 3 week versus count_3_pred

stack each plot vertially
Store that dashboard as an html file named model_dashboard.html

27 get all records in df_predict having primary_type=THEFT and ward=27, and show:
date, count_0, date+1 week, count_1, date+2 weeks, count_2, date+3 weeks, count_3 




