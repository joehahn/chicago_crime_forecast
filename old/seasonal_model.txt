seasonal_model.txt
by Joe Hahn
jmh.datasciences@gmail.com
2026-April-11

The following will train the so-called seasonal model on the monthly Chicago crimes data.

Execute the following completely from scratch and starting fresh.
Do not use any cached results, temporary files, or previously computed outputs. 
Do not gather any files using git.
Do not utlize any files previously stored in /tmp
Clear any cache files first, then execute everything fresh.

Begin by loading file data/crimes_monthly.csv into object df_monthly

And then set df_train = all records in df_monthly having TTV='train', 
keeping only the date, year, month, ward, primary_type, delta_count, count_0, count_1, count_2, count_3, count_4 columns.

Set df_test = all records in df_monthly having TTV='test', preserving the same columns. 
And set df_validate = all records in df_monthly having TTV='validate', preserving the same columns. 
And set df_forecast = all records in df_monthly having TTV='forecast', preserving the same columns. 
How many records are in df_train, df_test, df_validate, df_forecast?

Show 5 random records from df_train

Train an XGBoost machine learning model on df_train. 
That model's features are the year, month, ward, primary_type, delta_count, count_0 columns, 
and that model's target variable is count_1,
with df_test being the testing sample of data. Name that trained model seasonal_1.

Then train an XGBoost on df_train using the same features to predict count_2, name that trained model seasonal_2.
Then train an XGBoost on df_train using the same features to predict count_3, name that trained model seasonal_3.
Then train an XGBoost on df_train using the same features to predict count_4, name that trained model seasonal_4.

Use the seasonal_1 model to generate predictions on df_test, and append those predictions to
df_test as new column called 'count_1_pred'. Then do similar using the seasonal_2, seasonal_3, seasonal_4 models

Then do the same for df_validate.

Then do the same for df_forecast.

Show all records in df_validate having primary_type=THEFT and ward=27
Show all records in df_forecast having primary_type=THEFT and ward=27

Save models seasonal_1, seasonal_2, seasonal_3, seasonal_4 to a file or files.

Next, create a dashboard of model validation tables and plots:

Plot 1: Start with df_monthly, then groupby date, TTV and compute sum(count_0) as total_count.
Then sum the train + test timeseries and plot that sum versus date.
Then plot the validation timeseries versus date.
And plot the forecast timeseries versus date.
Use connected scatterplot and color code by TTV.

Table 1: Then use df_validate to show a table of model validatation scores for models seasonal_1, seasonal_2, seasonal_3, seasonal_4.
Include MAE and RMSE and R2 scores.

Table 2: Insert the feature importance's for models seasonal_1, seasonal_2, seasonal_3, seasonal_4 into a dataframe.
Convert that dataframe to string
Then preserve the first 5 characters of all values in that dataframe
Use that dataframe to show a table of each model's feature importance

plot 2: use df_validate to scatterplot of seasonal_1 predictions versus actuals. 
Use logarithmic horizontal axes ranging from 0.8 to 600.
Use logarithmic vertical axes ranging from 0.2 to 600.
Do not distinguish between the different crimes or wards.
Do not add 0.5 to the predictions or actuals.
Only include points whose predictions and actuals are > 0
Use opaque green dots for records whose count_0 are in the middlemost 80% of the data,
And opaque red dots for records whose count_0 are in the outermost 20%.
Also overplot y=x via a dashed line, and label that line 'prediction=actual'

plot 3: same as plot 1 but for seasonal_2

plot 4: same as plot 1 but for seasonal_3

plot 5: same as plot 1 but for seasonal_4

plot 6: use df_validate to show timeseries plots of summed count_0 for all THEFT records versus date.
Also put vertical errorbars on the count_0 curve, assume each errorbar extends up/down by sqrt(count_0)
Then overplot summed count_1_pred versus date + 1 month
Then overplot summed count_2_pred versus date + 2 month
Then overplot summed count_3_pred versus date + 3 month
Then overplot summed count_4_pred versus date + 4 month

Add legend to upper right corner of this plot

plot 7: same as plot 5 but for BURGLARY

plot 8: same as plot 7 but for ARSON

plot 9: use df_validate to show timeseries plots of summed count_0 for all primary_type for wards 27, 29, and 38 vesus date.
Also put vertical errorbars on the count_0 curve, assume each errorbar extends up/down by sqrt(count_0)
Then overplot summed count_1_pred versus date + 1 month
Then overplot summed count_2_pred versus date + 2 month
Then overplot summed count_3_pred versus date + 3 month
Then overplot summed count_4_pred versus date + 4 month
Use red for ward 27 plots.
Use blue for ward 29 plots.
Use green for ward 38 plots.
The plot's vertical axis should use logarithmic axis.
Add legend to upper right corner of this plot
Show as connected scatterplots

plot 10: read file data/crimes.csv and select all of the primar_type=THEFT records that occurred in March of 2026.
Use the latitude,longitude of those records to superimpose a heatmap of those THEFTS on top of a streetmap of chicago
Use logarithmic scaling to colorizing the THEFTS.
This plot's longitude's should range over -87.85 to -87.5 

Stack each plot or chart vertically.
Use no more than 10px of margin/padding between plots and tables. Set gap, margin, and padding to minimal values throughout.
Create a distinct, self-contained legend for each individual plot. Do not share or consolidate legends across plots. 
Set the vertical space between adjacent tables to 10px.
Also set the vertical space between a table and a plot at 10px.
Every chart must have its own legend embedded within it.
Store that dashboard as an html file named seasonal_model_dashboard.html

